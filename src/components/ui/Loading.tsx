const Loading = () => (
  <div className="flex items-center justify-center p-8">
    <div className="w-5 h-5 rounded-full border-2 border-gray-200 border-t-gray-400 dark:border-white/10 dark:border-t-white/30 animate-spin" />
  </div>
);

export default Loading;
